# ======== NETWORK LIBRARY - TCP/IP Communication ========
# This file is included for organization but network code is used directly by executables
# since there's no separate network compilation unit

# Note: TCP_Server.cpp and TCP_Client.cpp are compiled as part of Server and Client 
# executables, not as a separate library, because they contain main() functions
